import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nx-ionic-mobile',
  webDir: '../../dist/apps/nx-ionic-mobile',
  bundledWebRuntime: false,
};

export default config;
